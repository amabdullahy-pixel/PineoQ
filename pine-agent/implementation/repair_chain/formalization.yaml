# formalization contract — generated by formalize.pl (schema v1.1; meta/formalization_procedure.md)
schema_version: "1.1"
stage: formalization
predecessor: skill_routing
successor: pre_verification

formalization:
  request_id: 'req-a2f779518607'
  approved_routing_id: 'rout-3e209e603a09'
  formalization_id: 'form-43313af4902b'
  supersedes: null
  natural_language_request: 'Repair condition C-1 per Phase 15 repair rep-e44daf4b9966 and the recorded user decisions on the 3m chart timeframe: the signal condition is ''Pullback_Score crosses over 30'' where Pullback_Score is the ARO indicator normalized bounded-scale series supplied as an external chart input bound via input.source at chart level (input title Pullback Score ARO). Evaluation stays on confirmed bar close. Pine Script v6, same module architecture as impl-f56a05897f87.'
  # normalized_request: whitespace-normalized verbatim (engine scope; agent refinements go through supersedes revisions)
  normalized_request: 'Repair condition C-1 per Phase 15 repair rep-e44daf4b9966 and the recorded user decisions on the 3m chart timeframe: the signal condition is ''Pullback_Score crosses over 30'' where Pullback_Score is the ARO indicator normalized bounded-scale series supplied as an external chart input bound via input.source at chart level (input title Pullback Score ARO). Evaluation stays on confirmed bar close. Pine Script v6, same module architecture as impl-f56a05897f87.'
  variables:
    - { name: 'close', type: 'series float', description: 'built-in source token ''close'' detected in the request (verbatim)', source: 'request (verbatim)' }
    - { name: 'pullback_score', type: 'series float', description: 'external indicator series ''Pullback_Score'' referenced by C-1 — bound at chart level (input.source); no value invented', source: 'request (verbatim condition subject)' }
  formulas: []
  boolean_conditions:
    - { id: 'C-1', condition: 'Pullback_Score crosses over 30', meaning: 'verbatim request clause — semantics preserved, not reinterpreted', used_by: [] }
  assumptions: []
  ambiguities: []
  edge_cases:
    - { id: 'E-NA', case: 'na values during warm-up (insufficient history)', required_behavior: null }
    - { id: 'E-FIRST', case: 'first bars / insufficient history for lookbacks', required_behavior: null }
  timeframe:
    chart: '3m'
    calculation: null
  mtf: false
  required_data: []
  repaint_risk: 'low — mechanism: no repaint mechanism detected in the request'
  source_skills: ['pine-version-intelligence']
  verification_requirements: ['logic_valid', 'math_valid', 'variables_defined', 'assumptions_documented', 'ambiguities_resolved_or_recorded', 'edge_cases_checked', 'repaint_risk_classified']
  implementation_constraints: ['Target Pine Script v6 only (no v5/v4 syntax).', 'Boolean condition clauses C-* are verbatim requirements — preserve their semantics exactly.', 'No lookahead / future-leak: any request.security use requires an explicit user-approved lookahead policy.', 'Behavior-affecting ambiguities must be resolved_by_user before implementation may start.', 'Preserve the declared repaint classification (''low'') or change it only with user consent.']
  status: 'completed'
  blockers: []
  implementation_allowed: true
